interface:
  display_name: "Bugfender"
  short_description: "Investigate logs, crashes, and issues"
  default_prompt: "Use $bugfender to investigate the relevant Bugfender app, narrow the time range, and summarize the likely cause and next checks."

dependencies:
  tools:
    - type: "mcp"
      value: "bugfender"
      description: "Bugfender MCP server"

policy:
  allow_implicit_invocation: true
